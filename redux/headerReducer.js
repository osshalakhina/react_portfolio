let initialState = {
    nav: [
        {id: 1, link: 'Skill'},
        {id: 2, link: 'Works'},
        {id: 3, link: 'Contact'}
    ]
};
const headerReducer = (state = initialState, action) => {
    return state;
};
export default headerReducer;
